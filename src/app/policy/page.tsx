import { PolicyAdminClient } from "@/app/policy/policy-admin-client";

/**
 * Policy administration route.
 *
 * @returns Policy admin page.
 */
export default function PolicyPage(): React.ReactElement {
  return <PolicyAdminClient />;
}
