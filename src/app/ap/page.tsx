import { ApInvoicesClient } from "@/app/ap/ap-invoices-client";

/**
 * AP inbox route.
 *
 * @returns AP list page.
 */
export default function ApPage(): React.ReactElement {
  return <ApInvoicesClient />;
}
